# Installation

Create a conda environment using the requirements.yml file in the root directory:

    conda env create --file requirements.yml -n viscon-env

Activate the environment using the following commands:

    conda activate viscon-env

Run the application with

    python maintest.py